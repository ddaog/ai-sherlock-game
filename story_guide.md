# 🧩 오픈 탐색형 추리 게임 스토리 설계 가이드

---

## 핵심 설계 원칙: 충격과 울림

모든 스토리는 **플레이어가 진실을 밝힌 직후, 멍하게 화면을 바라보게 만드는** 순간을 반드시 설계해야 한다.

이 원칙은 두 축으로 구성된다:

### 충격 (Shock)
진실이 드러나는 순간, 이미 본 단서들이 완전히 다른 의미로 재해석되어야 한다.

- 믿었던 인물이 가해자였다
- 피해자가 사실 더 큰 죄를 저질렀다
- 범인의 동기가 단순한 탐욕이 아니었다
- 선한 의도가 비극적 결말로 이어졌다

### 울림 (Emotional Resonance)
사건 배후에 인간적 감정이 있어야 한다. 추리 게임이 아니라 **사람의 이야기**여야 한다.

- 범인이 완전히 나쁜 사람이 아니다 (또는 피해자가 완전히 선한 사람이 아니다)
- 단서 하나가 감정적으로 무너지게 만드는 순간을 설계한다
  (예: 마지막으로 발견되는 편지, 사진, 숨겨진 녹음, 지워진 메시지)
- 엔딩에서 플레이어는 범인을 지목하면서도 복잡한 감정을 느껴야 한다

### 측정 기준

스토리를 완성한 뒤 아래 질문에 모두 "예"로 답할 수 있어야 한다:

| 질문 | 의미 |
|------|------|
| "왜 그랬는지 이해는 된다" | 범인 동기에 인간적 설득력이 있는가? |
| "마지막 단서가 가슴에 남는다" | 감정적으로 울리는 결정적 증거가 존재하는가? |
| "결말을 알고 나서 처음부터 다시 보고 싶다" | 반전이 기존 정보를 재해석하게 만드는가? |

---

## 0. 파일 구조

새 스토리는 `data/stories/{id}/` 디렉토리 아래 아래 3개 파일을 작성한다.
(`embeddings.json`은 서버가 자동 생성하므로 직접 작성 불필요)

```
data/stories/{id}/
  ├── config.ts       ← 정답·파싱룰·엔딩·힌트 질문
  ├── evidence.json   ← 단서 레코드 배열
  └── display.ts      ← ASCII 씬·인물 시각화
```

완성 후 `data/registry.ts`의 스토리 목록에 등록해야 한다.

---

## 1. 사건 구조 (Case Layers)

사건은 최소 3개의 층위를 가져야 한다:

1. **표면 사건** — 겉으로 보이는 상황
2. **왜곡된 정보** — 오해 / 거짓 / 은폐된 사실
3. **실제 진실** — 플레이어가 도달해야 하는 결론

---

## 2. 공간 설계 (Spatial Design)

스토리는 **"장소" 단위로 분해한다**
(예: 집, 사무실, 골목, 병원 등)

- 장소는 최소 3개 이상
- 각 장소는 반드시 아래 요소 포함:
  - 단서 1개 이상
  - 인물 또는 흔적
  - 새로운 질문

- 모든 장소는 서로 연결된 구조로 설계한다
  → 특정 순서 없이도 접근 가능

---

## 3. 정보 단위 (Clue Fragment) — evidence.json

모든 정보는 **독립적인 조각(Fragment)** 형태로 `evidence.json`에 작성한다.

### 레코드 형식

```json
{
  "id": "001",
  "type": "INCIDENT_REPORT",
  "time_range": "22:45",
  "entities": ["김도윤", "별관 3층", "의식불명"],
  "tags": ["발견", "긴급"],
  "text": "실제 기록 내용 전문."
}
```

| 필드 | 설명 |
|------|------|
| `id` | 3자리 숫자 (001, 002 …), 순서대로 부여 |
| `type` | 아래 타입 목록 참고 |
| `time_range` | 해당 기록의 시간 또는 날짜 |
| `entities` | 이 기록에 등장하는 인물·장소·물체 |
| `tags` | 검색용 키워드 |
| `text` | 플레이어에게 공개되는 실제 기록 내용 |

### 사용 가능한 type

| 값 | 의미 |
|----|------|
| `INCIDENT_REPORT` | 사건 최초 보고서 |
| `ACCESS_LOG` | 출입·접근 기록 |
| `CCTV_LOG` | CCTV 영상 기록 |
| `EMAIL` | 이메일 |
| `MEMO` | 내부 메모·문서 |
| `CHAT_LOG` | 채팅·메신저 기록 |
| `AUTOPSY` | 부검·의료 기록 |
| `TESTIMONY` | 진술서 |
| `FINANCIAL_RECORD` | 금융·회계 기록 |
| `FORENSIC` | 과학 감식 결과 |

### 단서 역할

각 레코드는 아래 중 하나의 서사 역할을 가진다:

- **사실 제시 (Fact)** — 실제 일어난 일을 있는 그대로 전달
- **오해 유도 (Misdirection)** — 플레이어를 잘못된 방향으로 이끄는 정보
- **연결 트리거 (Link Trigger)** — 2~3개가 결합될 때 의미가 완성되는 조각

**권장 비율**: Fact 50% / Misdirection 25% / Link Trigger 25%

### 단서 설계 원칙

- 단독으로는 불완전해야 한다
- 2~3개가 결합될 때 의미가 완성된다

---

## 4. 인물 설계 (Suspect Design) — display.ts / config.ts

### 서사 조건

모든 주요 인물은 다음 3요소를 가진다:

- **동기 (Motive)**
- **기회 (Opportunity)**
- **숨기고 싶은 사실 (Secret)**

추가 조건:

- 각 인물은 최소 1개의 거짓 정보를 제공한다
- 동일 인물을 여러 번 조사할 때 정보가 갱신되도록 설계한다

### display.ts 인물 항목 형식

```ts
{
  name: "박지훈",
  role: "감사팀장",
  brief: "15년 경력의 감사 전문가. 냉정하고 원칙주의적으로 알려져 있다.",
  ascii: [
    // profile-ascii/ 디렉토리의 해당 .md 파일 내용을 그대로 복사
    "  ██████  ",
    " ██    ██ ",
    // ...
  ],
  queryHints: ["박지훈", "감사팀장", "지훈"],
  sourceMd: "businessmen.md",
}
```

| 필드 | 설명 |
|------|------|
| `name` | 인물 이름 |
| `role` | 직책/역할 |
| `brief` | 1~2문장 인물 소개 |
| `ascii` | `profile-ascii/` 내 `.md` 파일 내용 복사 (파일 하나당 인물 하나) |
| `queryHints` | `/인물 [이름]` 명령어 매칭용 키워드 목록 |
| `sourceMd` | 참조한 ascii 파일명 (기록용) |

### 사용 가능한 ascii 파일

`profile-ascii/` 디렉토리 내:
`beard.md`, `big men.md`, `businessmen.md`, `cat.md`, `dog.md`,
`hat men.md`, `men.md`, `nerd.md`, `old man.md`, `sunglassmen.md`,
`women copy.md`, `women.md`

---

## 5. 진행 구조 (Progression)

스토리는 **"순서"가 아니라 "이해도"로 진행된다**

- 특정 단서 조합을 획득하면:
  - 새로운 정보
  - 새로운 대화
  - 새로운 장소

이 열리는 구조로 설계한다

### 예시

- A 단서 + B 단서 → 새로운 인물 등장
- C 단서 확보 → 기존 인물의 진술 변화

---

## 6. 플레이어 행동 설계 (Player Actions)

플레이어의 핵심 행동은 3가지로 제한한다:

- 탐색 (Search)
- 비교 (Compare)
- 추론 (Infer)

- 플레이어가 스스로 다음 질문을 떠올리게 만들어야 한다

---

## 7. 반전 설계 (Twist Design)

반전은 **새로운 정보가 아니라 기존 정보의 재해석**이어야 한다

### 반전 발생 조건

- 이미 본 단서들이 다른 의미로 연결됨
- 플레이어가 스스로 깨닫는 구조

---

## 8. 결말 설계 (Resolution) — config.ts

플레이어가 직접 결론을 내리게 한다 (범인 지목, 사건 재구성 등)

정답은 반드시 모든 단서로 설명 가능해야 한다

### 정답 판별 구조

```ts
solution: {
  culprit: "박지훈",             // 단독 범인
  culprits: ["박지훈", "이서연"], // 공범 포함 시 모두 지정
  motive_keywords: ["비자금", "횡령", "도피"],   // 동기 파싱용
  method_keywords: ["약물", "수면제", "텀블러"], // 수법 파싱용
  required_records: ["011", "021", "025"],       // 정답에 필요한 핵심 레코드 (5개 내외 권장)
},
```

- `required_records`: 플레이어가 반드시 조회해야 하는 핵심 단서 ID 목록
- 이 레코드들을 확인하지 않으면 정답으로 인정하지 않는다

### 파싱 규칙

```ts
parsing: {
  suspectMentioned: /(박지훈|이서연|공범)/,
  hasMotive: /(비자금|횡령|돈|동기)/i,
  hasMethod: /(약물|수면제|방법|수법)/i,
  suspectNormalize: [{ from: /박팀장/g, to: "박지훈" }], // 별명 정규화 (선택)
},
```

### 엔딩 텍스트

```ts
ending: {
  solvedTitle: "사건 해결: 제목",
  solvedSummaryLines: [
    "결말 줄글 1",
    "결말 줄글 2",
    // 5~7줄 권장
  ],
  closedLine: "사건 상태: CLOSED (죄목)",
},
```

### 좋은 결말의 기준

- 납득 가능하다
- 놓친 단서가 떠오른다
- 다시 플레이하고 싶어진다

---

## 9. 힌트 질문 설계 (Next Questions) — config.ts

막힌 플레이어에게 제공할 힌트 질문을 `nextQuestions`에 작성한다.

```ts
nextQuestions: {
  motive: ["동기와 관련된 힌트 질문 1", "힌트 질문 2"],
  method: ["수법과 관련된 힌트 질문 1", "힌트 질문 2"],
  requiredRecordHint: {
    "011": "이 레코드를 떠올리게 할 질문",
    "021": "이 레코드를 떠올리게 할 질문",
  },
  crossCheck: [
    "두 단서를 연결하도록 유도하는 질문",
  ],
},
```

---

## 10. 현장 시각화 설계 (Scene Display) — display.ts

```ts
export const ASCII_SCENE = {
  title: "현장 타이틀",
  summary: "현장 한 줄 요약 (플레이어에게 첫 번째로 표시됨)",
  art: [
    // 현장 ASCII 아트 (선택, 없으면 생략)
    "┌─────────────┐",
    "│  현장 레이아웃  │",
    "└─────────────┘",
  ],
  layout: {
    // React 렌더링용 좌표 데이터 (선택)
    bands: [...],
    rooms: [...],
    pins: [...],
  },
  details: [
    "현장에서 발견된 주요 사항 1",
    "현장에서 발견된 주요 사항 2",
  ],
  focus: [
    "수사관이 집중해야 할 포인트 1",
    "수사관이 집중해야 할 포인트 2",
  ],
};
```

| 필드 | 필수 | 설명 |
|------|------|------|
| `title` | ✅ | 현장 타이틀 |
| `summary` | 권장 | 현장 한 줄 설명 |
| `art` | 선택 | ASCII 아트 배열 |
| `layout` | 선택 | React 렌더링용 좌표 |
| `details` | 선택 | 현장 세부 정보 |
| `focus` | ✅ | 수사 집중 포인트 목록 |
